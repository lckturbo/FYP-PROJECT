using UnityEngine;

public abstract class InteractionController<T> : MonoBehaviour where T : BoardEntity
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
